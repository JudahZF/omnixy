# Example sops configuration. Copy to .sops.yaml at repo root and edit recipients.
# Docs: https://github.com/getsops/sops

keys:
  - &admin_age age1exampleexampleexampleexampleexampleexampleexampleexample

creation_rules:
  - path_regex: secrets/.*\.(yaml|yml)$
    key_groups:
      - age:
          - *admin_age
